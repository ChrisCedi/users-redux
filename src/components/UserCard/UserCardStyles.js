import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  container: {
    height: 200,
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  },
}));

export { useStyles };
